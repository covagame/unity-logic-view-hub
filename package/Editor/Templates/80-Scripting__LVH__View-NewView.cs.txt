using System; 
using UnityEngine;
using R3;
using Covagame.LVH.Common;
using Assert = UnityEngine.Assertions.Assert;

#ROOTNAMESPACEBEGIN#

    public class #SCRIPTNAME# : MonoBehaviour, IView<TProperty>
    {
        private readonly CompositeDisposable _disposable = new CompositeDisposable();

        public void Dispose() => _disposable.Dispose();

        private void OnDestroy() =>Dispose();
        
        private void Reset()
        {
            
        }
        private void Awake()
        {
            
        }
        void IView<TProperty>.Bind(TProperty property)
        {
            // TODO: Implementation Binding

        }
    }

#ROOTNAMESPACEEND#
